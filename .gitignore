.venv/
__pycache__/
*.pyc
*.pyo
.env
.kite_session.json
*.db
*.sqlite
*.csv
*.xlsx
.DS_Store

# Node (Tailwind CLI build tooling)
node_modules/
# NOTE: static/tailwind.css IS committed — it's the built output required to serve the app
# Rebuild with: npm run build:css

# Runtime state — contains live trade data, never commit
.state_snapshot.json
trade_log.json
paper_trades.db

# Crude trader runtime state & user-tuned settings
.crude_snapshot.json
crude_trade_log.json
crude_settings.json

# Azure deployment credentials (contains sensitive service principal data)
azure-config.txt
-e 
# Backup files
*.backup_*
pattern_detector_old.py
